{"input": {"a": [1, 2, 3, 4], "b": [10, 20, 30, 40], "_script": "\nusing CUDA\nusing JSON3\n\nfunction vadd_kernel!(y, a, b)\n    i = (blockIdx().x - 1) * blockDim().x + threadIdx().x\n    if i <= length(y)\n        @inbounds y[i] = a[i] + b[i]\n    end\n    return nothing\nend\n\nfunction run_cuda(a_host, b_host)\n    n = length(a_host)\n    a_dev = CuArray(a_host)\n    b_dev = CuArray(b_host)\n    y_dev = CUDA.zeros(Float32, n)\n    threads = 256\n    blocks = cld(n, threads)\n    @cuda threads=threads blocks=blocks vadd_kernel!(y_dev, a_dev, b_dev)\n    CUDA.synchronize()\n    return Array(y_dev)\nend\n\nfunction main()\n    input = JSON3.read(ARGS[1])\n    a_host = Float32.(input.a)\n    b_host = Float32.(input.b)\n    if !CUDA.functional()\n        println(JSON3.write(Dict(\"error\" => \"CUDA is not functional on this node\")))\n        exit(1)\n    end\n    y_host = run_cuda(a_host, b_host)\n    dev_name = CUDA.name(CUDA.device())\n    println(JSON3.write(Dict(\"result\" => y_host, \"device\" => dev_name, \"engine\" => \"cuda-only-healthcheck\")))\nend\n\nmain()\n"}}